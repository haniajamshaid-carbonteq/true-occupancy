# macOS
.DS_Store
._*

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Dependencies (if a bundler is added later)
node_modules/
dist/
build/

# Playwright — the E2E test tooling is kept out of the repo. The app runtime
# is buildless; package.json / package-lock.json existed only for these tests.
tests/
playwright.config.ts
package.json
package-lock.json
test-results/
playwright-report/
playwright/.cache/
blob-report/

# Env / secrets
.env
.env.*
!.env.example

# OS / misc
Thumbs.db
